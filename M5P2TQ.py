# Tamer Quran
# 8/5/2021
# This program puts out a random odd number between 1 and 100
import random
# the third number makes it so that the number that is outputted is either odd or even depending on the starting number.
a = random.randrange(1, 100, 2)
print(a)
